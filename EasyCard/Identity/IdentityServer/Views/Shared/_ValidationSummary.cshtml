@inject CommonLocalizationService localizer

@if (ViewContext.ModelState.IsValid == false)
{
    <div class="alert alert-danger">
        <strong>@localizer.Get("Error")</strong>
        <div asp-validation-summary="All" class="danger"></div>
    </div>
}